name: DevSecOps CI/CD Pipeline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

env:
  ACR_NAME: ${{ vars.ACR_NAME }}
  IMAGE_TAG: ${{ github.sha }}

jobs:
  # ==============================================================================
  # JOB 1: SECURITY GATES
  # ==============================================================================
  security-gates:
    name: Security & Secret Detection
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Gitleaks Secret Scan
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ==============================================================================
  # JOB 2: BUILD, SCAN & PUSH
  # ==============================================================================
  build-and-push:
    name: Build, Scan & Push
    needs: security-gates
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install Dependencies
        run: npm ci

      - name: Semgrep SAST
        run: |
          pip install semgrep
          semgrep scan --config "p/typescript" --error .

      - name: Trivy Dependency Scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: "fs"
          scan-ref: "."
          severity: "CRITICAL"
          exit-code: "1"

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Install Podman
        run: |
          sudo apt-get update
          sudo apt-get install -y podman

      - name: Login to ACR with Podman
        run: |
          TOKEN=$(az acr login --name ${{ env.ACR_NAME }} --expose-token --query accessToken -o tsv)
          echo "$TOKEN" | podman login ${{ env.ACR_NAME }}.azurecr.io \
            --username 00000000-0000-0000-0000-000000000000 \
            --password-stdin

      - name: Build Image with Podman
        run: |
          podman build \
            --layers \
            -t ${{ env.ACR_NAME }}.azurecr.io/todo-api:${{ env.IMAGE_TAG }} \
            -f ./deploy/Dockerfile \
            .

      - name: Trivy Image Scan
        run: |
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sh -s -- -b /usr/local/bin
          trivy image \
            --exit-code 1 \
            --severity CRITICAL \
            --ignore-unfixed \
            ${{ env.ACR_NAME }}.azurecr.io/todo-api:${{ env.IMAGE_TAG }}

      - name: Push Image with Podman
        run: |
          podman push ${{ env.ACR_NAME }}.azurecr.io/todo-api:${{ env.IMAGE_TAG }}

  # ==============================================================================
  # JOB 3: ACR CLEANUP (COST OPTIMIZATION)
  # ==============================================================================
  cleanup-acr:
    name: ACR Cleanup
    needs: build-and-push
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Purge Untagged Images
        run: |
          echo "Starting cleanup for registry: $ACR_NAME"
          
          cleanup_repo() {
            local REPO=$1
            echo "Analyzing repository: $REPO"
            
            local DANGLING_MANIFESTS=$(az acr repository show-manifests \
              --name $ACR_NAME \
              --repository $REPO \
              --query "[?tags==null].digest" \
              -o tsv)
            
            if [ -z "$DANGLING_MANIFESTS" ]; then
              echo "  - No untagged manifests found."
            else
              echo "  - Found dangling manifests. Initiating deletion..."
              for DIGEST in $DANGLING_MANIFESTS; do
                echo "    - Deleting $REPO@$DIGEST"
                az acr repository delete \
                  --name $ACR_NAME \
                  --image "$REPO@$DIGEST" \
                  --yes
              done
            fi
          }

          REPOS=$(az acr repository list --name $ACR_NAME -o tsv 2>/dev/null || echo "")
          
          if [ -n "$REPOS" ]; then
            for R in $REPOS; do
              cleanup_repo $R
            done
          fi
          
          echo "Cleanup complete."
